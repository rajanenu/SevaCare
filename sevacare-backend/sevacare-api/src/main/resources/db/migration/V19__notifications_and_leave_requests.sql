-- =============================================================
-- V19: Leave Requests + In-app Notifications
-- Per-tenant tables (leave_request, app_notification) are
-- created dynamically by TenantAdminSchemaInitializer on
-- startup for every active tenant schema. No DDL here.
-- =============================================================
SELECT 1;
